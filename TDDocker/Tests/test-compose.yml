services:
  web:
    image: nginx:latest
    ports:
      - "8080:80"
  echo:
    image: ealen/echo-server:latest
    ports:
      - "3000:80"
  osc-test:
    build: ../docker/osc-test
    ports:
      - "127.0.0.1:9001:9001/udp"
